#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int numFrequency[45] = {0, };	// 931~961회차 당첨 빈도수
int random[210] = {0, };		//빈도수 만큼 1~45 번호 입력 배열

int main(void) {

}

void lFrequency() {				//random[210]에 빈도수만큼 번호 채우는 함수
	int i, j, k = 0;

}